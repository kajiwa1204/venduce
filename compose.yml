services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: venduce_user
      POSTGRES_PASSWORD: venduce_password
      POSTGRES_DB: venduce_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U venduce_user -d venduce_db']
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    env_file:
      - .env
    ports:
      - '8000:8000'
    environment:
      DATABASE_URL: postgresql://venduce_user:venduce_password@postgres:5432/venduce_db
      APP_ENV: ${APP_ENV:-development}
      MAIL_HOST: mailhog
      MAIL_PORT: '1025'
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - backend_storage:/app/storage
    healthcheck:
      test: ['CMD-SHELL', 'python -c "import urllib.request; urllib.request.urlopen(''http://localhost:8000/api/health'').read()"']
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  frontend:
    build: ./frontend
    ports:
      - '3000:3000'
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules # ホストと分離
    env_file:
      - .env
    environment:
      - NODE_ENV=${APP_ENV:-development}
      - APP_ENV=${APP_ENV:-development}
      - BACKEND_INTERNAL_URL=http://backend:8000
    command: sh -c 'if [ "$$APP_ENV" = "production" ]; then npm start; else npm run dev; fi'
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
      - frontend
      - mailhog

  mailhog:
    image: mailhog/mailhog
    ports:
      - '8025:8025' # UI
      - '1025:1025' # SMTP

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    profiles:
      - production
    depends_on:
      - nginx
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}

volumes:
  postgres_data:
  frontend_node_modules:
  backend_storage:
